enum Gender { none, male, female }
enum SettingsMenuAction { logout }
enum RecommendationLevel { info, warning }
enum BottomBarItem { recommendations, statistics, profile }
enum MetricsType { bpm, steps }
